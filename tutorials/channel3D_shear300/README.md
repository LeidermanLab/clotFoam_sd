# 3D channel with wall shear of 300 1/s
This test case was used to validate the shear dependent platelet aggregation model. Build the mesh by executing:

~~~
./Allmesh
~~~

This ensures proper creation of the `injuryWalls` patch via the `topoSet` and `createPatch` tools. 